<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Android 6-12: broad external storage read permission.
        maxSdkVersion=32 prevents it appearing in the permission dialog on API 33+.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ (API 33+): granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!--
        Android 14+ (API 34+): allows access to user-selected photos/videos only
        (limited access via photo picker).
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

</manifest>
